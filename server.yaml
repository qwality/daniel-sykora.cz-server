servers:
  this:
    commands:
      update: "git pull && cp nginx/nginx.conf /etc/nginx/nginx.conf && chmod"
  nginx:
    commands:
      start: nginx
      stop: "nginx -s stop"
      restart: "nginx -s reload"