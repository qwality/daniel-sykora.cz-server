{
    "servers": {
        "this": {
            "commands": {
                "update": "git stash && git pull && cp nginx/nginx.conf /etc/nginx/nginx.conf"
            }
        },
        "nginx": {
            "commands": {
                "start": "nginx",
                "stop": "nginx -s stop",
                "restart": "nginx -s reload"
            }
        }
    }
}